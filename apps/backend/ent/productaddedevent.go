// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/SURF-Innovatie/MORIS/ent/event"
	"github.com/SURF-Innovatie/MORIS/ent/productaddedevent"
	"github.com/google/uuid"
)

// ProductAddedEvent is the model entity for the ProductAddedEvent schema.
type ProductAddedEvent struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// ProductID holds the value of the "product_id" field.
	ProductID uuid.UUID `json:"product_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ProductAddedEventQuery when eager-loading is set.
	Edges               ProductAddedEventEdges `json:"edges"`
	event_product_added *uuid.UUID
	selectValues        sql.SelectValues
}

// ProductAddedEventEdges holds the relations/edges for other nodes in the graph.
type ProductAddedEventEdges struct {
	// Event holds the value of the event edge.
	Event *Event `json:"event,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// EventOrErr returns the Event value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProductAddedEventEdges) EventOrErr() (*Event, error) {
	if e.Event != nil {
		return e.Event, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: event.Label}
	}
	return nil, &NotLoadedError{edge: "event"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProductAddedEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case productaddedevent.FieldID:
			values[i] = new(sql.NullInt64)
		case productaddedevent.FieldProductID:
			values[i] = new(uuid.UUID)
		case productaddedevent.ForeignKeys[0]: // event_product_added
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProductAddedEvent fields.
func (_m *ProductAddedEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case productaddedevent.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case productaddedevent.FieldProductID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field product_id", values[i])
			} else if value != nil {
				_m.ProductID = *value
			}
		case productaddedevent.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field event_product_added", values[i])
			} else if value.Valid {
				_m.event_product_added = new(uuid.UUID)
				*_m.event_product_added = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ProductAddedEvent.
// This includes values selected through modifiers, order, etc.
func (_m *ProductAddedEvent) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryEvent queries the "event" edge of the ProductAddedEvent entity.
func (_m *ProductAddedEvent) QueryEvent() *EventQuery {
	return NewProductAddedEventClient(_m.config).QueryEvent(_m)
}

// Update returns a builder for updating this ProductAddedEvent.
// Note that you need to call ProductAddedEvent.Unwrap() before calling this method if this ProductAddedEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ProductAddedEvent) Update() *ProductAddedEventUpdateOne {
	return NewProductAddedEventClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ProductAddedEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ProductAddedEvent) Unwrap() *ProductAddedEvent {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProductAddedEvent is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ProductAddedEvent) String() string {
	var builder strings.Builder
	builder.WriteString("ProductAddedEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("product_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ProductID))
	builder.WriteByte(')')
	return builder.String()
}

// ProductAddedEvents is a parsable slice of ProductAddedEvent.
type ProductAddedEvents []*ProductAddedEvent
