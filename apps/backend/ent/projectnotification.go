// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/SURF-Innovatie/MORIS/ent/projectnotification"
	"github.com/SURF-Innovatie/MORIS/ent/user"
	"github.com/google/uuid"
)

// ProjectNotification is the model entity for the ProjectNotification schema.
type ProjectNotification struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ProjectID holds the value of the "project_id" field.
	ProjectID uuid.UUID `json:"project_id,omitempty"`
	// Message holds the value of the "message" field.
	Message string `json:"message,omitempty"`
	// SentAt holds the value of the "sent_at" field.
	SentAt time.Time `json:"sent_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ProjectNotificationQuery when eager-loading is set.
	Edges                     ProjectNotificationEdges `json:"edges"`
	project_notification_user *uuid.UUID
	selectValues              sql.SelectValues
}

// ProjectNotificationEdges holds the relations/edges for other nodes in the graph.
type ProjectNotificationEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ProjectNotificationEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProjectNotification) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case projectnotification.FieldMessage:
			values[i] = new(sql.NullString)
		case projectnotification.FieldSentAt:
			values[i] = new(sql.NullTime)
		case projectnotification.FieldID, projectnotification.FieldProjectID:
			values[i] = new(uuid.UUID)
		case projectnotification.ForeignKeys[0]: // project_notification_user
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProjectNotification fields.
func (_m *ProjectNotification) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case projectnotification.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case projectnotification.FieldProjectID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field project_id", values[i])
			} else if value != nil {
				_m.ProjectID = *value
			}
		case projectnotification.FieldMessage:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field message", values[i])
			} else if value.Valid {
				_m.Message = value.String
			}
		case projectnotification.FieldSentAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field sent_at", values[i])
			} else if value.Valid {
				_m.SentAt = value.Time
			}
		case projectnotification.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field project_notification_user", values[i])
			} else if value.Valid {
				_m.project_notification_user = new(uuid.UUID)
				*_m.project_notification_user = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ProjectNotification.
// This includes values selected through modifiers, order, etc.
func (_m *ProjectNotification) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the ProjectNotification entity.
func (_m *ProjectNotification) QueryUser() *UserQuery {
	return NewProjectNotificationClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this ProjectNotification.
// Note that you need to call ProjectNotification.Unwrap() before calling this method if this ProjectNotification
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ProjectNotification) Update() *ProjectNotificationUpdateOne {
	return NewProjectNotificationClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ProjectNotification entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ProjectNotification) Unwrap() *ProjectNotification {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProjectNotification is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ProjectNotification) String() string {
	var builder strings.Builder
	builder.WriteString("ProjectNotification(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("project_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ProjectID))
	builder.WriteString(", ")
	builder.WriteString("message=")
	builder.WriteString(_m.Message)
	builder.WriteString(", ")
	builder.WriteString("sent_at=")
	builder.WriteString(_m.SentAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ProjectNotifications is a parsable slice of ProjectNotification.
type ProjectNotifications []*ProjectNotification
